# @vvmp/trust-cli

Command-line tools for VVMP.

Use this package for validation, summaries, registry publication, recovery, C2PA export, C2PA verification, and trusted-lane diagnostics.

```sh
npm install --save-dev @vvmp/trust-cli
npx trust validate manifest.json
```

See the root repository package integration guide for the full package map.
